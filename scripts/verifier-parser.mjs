/**
 * Repository entry point for the strict verifier parser.
 * The implementation lives with the proposal skill so copied skills remain
 * self-contained; benchmark code continues importing this stable path.
 */
export * from '../skills/proposal/scripts/verifier-parser.mjs';

# Normalize line endings so parsers, hashes, and generated adapters are
# identical on every platform. A CRLF checkout previously changed which
# skills the structural contract and the routing eval could see.
* text=auto eol=lf

*.jpg binary
*.png binary
*.pdf binary
